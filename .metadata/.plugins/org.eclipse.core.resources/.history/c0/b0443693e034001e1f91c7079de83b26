package com.pwright.rest.webservices.restfulwebservices.user;

import java.time.LocalDate;
import java.util.ArrayList;
import java.util.List;

import org.springframework.stereotype.Component;

@Component
public class UserDaoService {
	
	private static List<User> users = new ArrayList<>();
	
	static {
		users.add(new User(1, "Patrick", LocalDate.now().minusYears(30)));
		users.add(new User(1, "Ellen", LocalDate.now().minusYears(28)));
		users.add(new User(1, "Chris", LocalDate.now().minusYears(26)));
	}
	
	public List<User> findAll() {
		
	}
	
	public User save (User user) {
		
	}
	
	public User findOne(int id) {
		
	}

}
